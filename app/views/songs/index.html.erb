<ul>
<% @songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %></li>
  <li><%= link_to song.artist, artist_path(artist) %></li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
